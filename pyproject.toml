[tool.poetry]
name = "helmkeeper"
version = "0.1.0"
description = ""
authors = ["Marco A. Da Silva A. <marcoadasilvaa@gmail.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
semver = "^3.0.4"
pyyaml = "^6.0.2"
requests = "^2.32.3"
uuid = "^1.30"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0"
pytest-cov = "^4.1"
black = "^25.12"
ruff = "^0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
